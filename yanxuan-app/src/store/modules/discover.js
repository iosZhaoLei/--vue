
import {fetchGet} from 'fetch'
import api from 'api'

const state = {

}

const mutations = {
    
}

const actions = {
    
}

export default {
    namespace:true,
    state,
    mutations,
    actions
}

